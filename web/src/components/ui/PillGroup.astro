---
import { pillGroup } from '../../lib/pill-styles'

interface Props {
  class?: string
}
const { class: className } = Astro.props
---

<div class:list={[pillGroup(), className]}>
  <slot />
</div>
