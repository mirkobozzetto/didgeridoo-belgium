// Item de popover (menu compte, burger mobile, menu agenda) : une seule
// définition pour tout le site.
export function menuItem(): string {
  return (
    'block w-full px-4 py-2.5 text-left text-[13px] text-stone-600 ' +
    'transition-colors hover:bg-stone-50 hover:text-stone-900'
  )
}
